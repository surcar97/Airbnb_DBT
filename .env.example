# ============================================================================
# DBT ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# This file lists all environment variables needed for dbt connection profiles.
# 
# USAGE:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Fill in the values for your target warehouse (only one section needed)
# 3. Load the variables: source .env (Linux/Mac) or use a tool like python-dotenv
# 4. Ensure .env is in .gitignore (never commit actual credentials)
#
# NOTE: You only need to set variables for ONE warehouse adapter at a time.
# Uncomment and configure the section matching your target warehouse.
# ============================================================================

# ============================================================================
# POSTGRESQL ENVIRONMENT VARIABLES
# ============================================================================
DBT_POSTGRES_HOST=localhost
DBT_POSTGRES_PORT=5432
DBT_POSTGRES_USER=your_postgres_username
DBT_POSTGRES_PASSWORD=your_postgres_password
DBT_POSTGRES_DBNAME=airbnb
DBT_POSTGRES_SCHEMA=dev
DBT_POSTGRES_THREADS=4

# ============================================================================
# SQL SERVER ENVIRONMENT VARIABLES
# ============================================================================
# DBT_SQLSERVER_DRIVER=ODBC Driver 17 for SQL Server
# DBT_SQLSERVER_SERVER=localhost
# DBT_SQLSERVER_PORT=1433
# DBT_SQLSERVER_USER=your_sqlserver_username
# DBT_SQLSERVER_PASSWORD=your_sqlserver_password
# DBT_SQLSERVER_DATABASE=airbnb
# DBT_SQLSERVER_SCHEMA=dev
# DBT_SQLSERVER_THREADS=4

# ============================================================================
# SNOWFLAKE ENVIRONMENT VARIABLES
# ============================================================================
# DBT_SNOWFLAKE_ACCOUNT=your_account_locator
# DBT_SNOWFLAKE_USER=your_snowflake_username
# DBT_SNOWFLAKE_PASSWORD=your_snowflake_password
# DBT_SNOWFLAKE_ROLE=TRANSFORM
# DBT_SNOWFLAKE_DATABASE=AIRBNB
# DBT_SNOWFLAKE_WAREHOUSE=COMPUTE_WH
# DBT_SNOWFLAKE_SCHEMA=DEV
# DBT_SNOWFLAKE_THREADS=4

# ============================================================================
# DATABRICKS ENVIRONMENT VARIABLES
# ============================================================================
# DBT_DATABRICKS_HOST=your_workspace.cloud.databricks.com
# DBT_DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your_warehouse_id
# DBT_DATABRICKS_TOKEN=your_personal_access_token
# DBT_DATABRICKS_SCHEMA=dev
# DBT_DATABRICKS_THREADS=4
# DBT_DATABRICKS_CATALOG=main
